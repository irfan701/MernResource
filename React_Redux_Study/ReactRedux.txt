https://medium.com/how-to-react/use-npm-watch-to-auto-build-your-reactjs-app-6ed0e5d6cb00

Pillars of redux

******STORE =>OBJ [for 1 single App for 1 will be 1 store](crud)
	getState() =>Returns the current state of the store
	dispatch() =>It's an action. it's the only way to update the application state
		     STORE E ACTION KORTEY SAYLE DISPATCH

******ACTION
		
		
******REDUCERS
	
	 Reducers are PURE functions that specify how the application's state changes

React-Redux

# Redux + Plain JS template
npx create-react-app my-app --template redux






 Manually

	install React
	install Redux 
	set up a Redux store